services:
  - type: web
    name: scramjet-demo
    runtime: node
    buildCommand: corepack enable && pnpm install
    startCommand: pnpm start
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
